"""unitccl: run / profile / plot Bine-tree NCCL collective benchmarks.

Import package is `unitccl_cli` (the console command is still `unitccl`) so
this never collides with the compiled per-project `fastest` pybind11
extension, which is conventionally also named `unitccl`.
"""

__version__ = "0.1.0"
